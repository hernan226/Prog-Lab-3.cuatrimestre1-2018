var animales;
(function (animales) {
    var Animal = /** @class */ (function () {
        function Animal(nombre) {
            this.nombre = nombre;
        }
        return Animal;
    }());
    animales.Animal = Animal;
})(animales || (animales = {}));
